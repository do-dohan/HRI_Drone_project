build/ # ROS 2의 빌드 결과물이 담긴 폴더로, 도커 안에서 새로 만들 것이므로 무시합니다.
install/ # 설치된 실행 파일들이 모이는 폴더이며, 도커 내부 환경과 충돌할 수 있어 무시합니다.
log/ # 빌드 과정에서 생긴 기록 파일들로, 용량만 차지하므로 제외합니다.
.pixi/ # 내 컴퓨터용 라이브러리 환경입니다. 도커 안에서 따로 설치할 것이므로 무시합니다.
.git/ # 버전 관리 기록들입니다. 이미지 용량을 크게 줄이기 위해 제외하는 것이 좋습니다.
*.lock # 특정 시점의 상태를 잠근 파일들이나, 도커 빌드 시 새로 생성되게 유도합니다.