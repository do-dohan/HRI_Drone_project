# --- C++ / ROS 2 빌드 결과물 ---
# 코드를 컴파일해서 만든 실행 파일들이 모이는 곳입니다. (자동 생성됨)
ros_ws/build/
# 빌드가 끝난 파일들이 실제로 설치되는 경로입니다.
ros_ws/install/
# 프로그램을 돌릴 때 생기는 각종 기록(로그)들입니다.
ros_ws/log/

# 컴파일 과정에서 생기는 중간 단계의 '오브젝트' 파일입니다.
*.o
# 다른 프로그램들이 함께 쓰는 '공유 라이브러리' 파일입니다.
*.so

# --- 파이썬 관련 캐시 ---
# 파이썬이 실행 속도를 높이기 위해 만드는 임시 폴더입니다.
__pycache__/
# 파이썬 코드가 기계어로 번역된 결과물입니다.
*.pyc

# --- 시스템 및 에디터 설정 ---
# 개별 사용자의 VS Code 설정입니다. (팀원마다 설정이 다를 수 있어 제외)
.vscode/
# 맥(macOS) 사용자가 폴더를 열 때 생기는 시스템 파일입니다.
.DS_Store
# 터미널에서 파일을 수정할 때 생기는 임시 복사본입니다.
*.swp
.thumbs